spring:
  application:
    name: carApi
  datasource:
    url: jdbc:postgresql://localhost:5432/car
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
